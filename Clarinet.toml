[project]
name = 'TIME-LIMITED-ACCESS'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.TIME-LIMITED-ACCESS]
path = 'contracts/TIME-LIMITED-ACCESS.clar'
clarity_version = 4
epoch = 'latest'
[repl.analysis]
passes = [
    'check_checker',
    'call_checker',
]

[repl.analysis.lint_groups]

[repl.analysis.lints]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
